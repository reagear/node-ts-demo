/**
 * 两数相减
 * @param {number} a 入参a
 * @param {number} b 入参b
 * @returns {number} 结果
 */
function minus(a: number, b: number): number {
    return a - b
}

/**
 * 自增
 * @param {number} a 入参
 * @returns {number} 结果
 */
function addOne(a: number) :number{
    return a + 1
}

export {minus,addOne}